import { ExceptionalTimeMst } from './exceptional-time-mst';

describe('ExceptionalTimeMst', () => {
  it('should create an instance', () => {
    expect(new ExceptionalTimeMst()).toBeTruthy();
  });
});
